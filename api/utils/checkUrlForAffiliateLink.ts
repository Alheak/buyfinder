export default async function checkUrlForAffiliateLink (url: string): Promise<string | null> {
  let link = ''

  try {
    
  } catch (error) {
    console.error(`Can't check url ${url} for affiliate link:`, error)
  }

  return link
}
